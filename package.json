{
  "name": "nexui",
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "dev:mobile": "pnpm --filter @nexui/mobile dev",
    "dev:api": "pnpm --filter @nexui/api dev",
    "dev:web": "pnpm --filter @nexui/mobile web",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "lint:fix": "pnpm -r --if-present run lint:fix",
    "fix": "pnpm lint:fix && pnpm format",
    "typecheck": "turbo run typecheck",
    "test": "node --experimental-strip-types --test tests/*.test.mjs",
    "eval:intent": "node --experimental-strip-types scripts/eval-intent.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@nexui/config": "workspace:*",
    "prettier": "^3.6.2",
    "turbo": "^2.5.8"
  },
  "prettier": "@nexui/config/prettier"
}
