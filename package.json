{
  "name": "flowstate",
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "dev:mobile": "pnpm --filter @flowstate/mobile dev",
    "dev:api": "pnpm --filter @flowstate/api dev",
    "dev:web": "pnpm --filter @flowstate/mobile web",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@flowstate/config": "workspace:*",
    "prettier": "^3.6.2",
    "turbo": "^2.5.8"
  },
  "prettier": "@flowstate/config/prettier"
}
